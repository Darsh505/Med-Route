# ──────────────────────────────────────────────
# requirements.txt — Med Route Backend
# Python 3.12+
# ──────────────────────────────────────────────

# ── Web Framework ─────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0

# ── Database (Async PostgreSQL + PostGIS) ─────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
geoalchemy2==0.15.2
psycopg2-binary==2.9.9          # For Alembic migrations (sync)

# ── Migrations ────────────────────────────────
alembic==1.13.3

# ── Validation & Config ───────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2

# ── Auth & Security ───────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12        # For form data / file uploads

# ── Redis (Caching + Rate Limiting) ───────────
redis[hiredis]==5.1.0
aioredis==2.0.1

# ── Google AI ─────────────────────────────────
google-generativeai==0.8.3

# ── HTTP Client (for geocoding + PMJAY) ───────
httpx==0.27.2
aiohttp==3.10.8

# ── Geo / Location ────────────────────────────
geopy==2.4.1                    # Nominatim geocoding

# ── Data Processing ───────────────────────────
pandas==2.2.3                   # For CSV parsing in data pipeline
numpy==2.1.1

# ── WebSockets (SOS real-time alerts) ─────────
websockets==13.1

# ── Utilities ─────────────────────────────────
python-slugify==8.0.4           # Auto-generate hospital URL slugs
python-dotenv==1.0.1
structlog==24.4.0               # Structured JSON logging
tenacity==9.0.0                 # Retry logic for external APIs

# ── Testing ───────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2                   # Test client for FastAPI
factory-boy==3.3.1              # Test data factories
